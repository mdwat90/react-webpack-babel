const INITIAL_STATE = {
  currentDoc: null,
  authenticated: false,
  userInfo: null,
};

import { combineReducers } from 'redux';
// import todoLists from './todoLists'

export default combineReducers({
  //   todoLists
});
