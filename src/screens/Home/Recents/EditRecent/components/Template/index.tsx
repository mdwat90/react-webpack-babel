import React from 'react';
import { Typography } from '@material-ui/core';

const Template = () => {
  return (
    <div>
      <Typography variant="h5">Recents Template</Typography>
    </div>
  );
};

export default Template;
