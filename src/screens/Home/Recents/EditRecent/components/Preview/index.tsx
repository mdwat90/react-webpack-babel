import React from 'react';
import { Typography } from '@material-ui/core';

const Preview = () => {
  return (
    <div>
      <Typography variant="h5">Recents Preview</Typography>
    </div>
  );
};

export default Preview;
